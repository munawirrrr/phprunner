{
	"type": "EVENT_BUTTON",
	"name": "reset_password",
	"serverCode": "// password default\n$newPassword = \"123456\";\n\n// hash sesuai BCRYPT (sesuai setting PHPRunner)\n$hashed = password_hash($newPassword, PASSWORD_DEFAULT);\n\n// loop semua record terpilih\nwhile( $data = $button->getNextSelectedRecord() )\n{\n    $id = (int)$data[\"ID\"];\n\n    $sql = \"UPDATE user SET password='\" . $hashed . \"' WHERE ID=\" . $id;\n\n    DB::Exec($sql);\n}",
	"afterJsCode": "$(document).ready(function ()\n                            {\n                            \t   alert(\"password Sudah Reset\");\n                            });"
}